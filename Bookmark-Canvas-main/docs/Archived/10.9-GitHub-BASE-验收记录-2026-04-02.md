# 10.9 GitHub BASE 三方 Merge 验收记录（2026-04-02）

范围：`docs/统一JSON三方直通谨慎改造计划书.md` 第 10.9 节（R1~R4）

状态说明：

- `未测`：尚未执行
- `通过`：执行结果满足验收标准
- `失败`：执行结果不满足验收标准（需附问题单）

## 0. 环境与前置

- 仓库/分支：____________
- 插件版本/commit：____________
- 测试人：____________
- 测试日期：____________
- 同步目录（obsidianExportRoot）：____________

## 1. 用例与结果

### R1 双端都改 + base 可用

验收标准：空白卡源码三方 merge 结果稳定，且不再误用当前值作 base。

- 执行步骤：
1. 先完成一次稳定同步，确保 `prevSyncIndex.files[path].sha` 已建立。
2. 在本地与云端分别修改同一空白卡 `markdownSource`（制造双端都改）。
3. 执行同步，观察冲突处理与最终源码。
- 预期：
1. 使用 GitHub BASE 参与 merge。
2. 不出现“把 current 当 base”的错误结果。
- 结果：`未测`
- 备注：____________

### R2 双端都改 + base 缺失

验收标准：正确进入 `conflict-copy/manual`，不出现静默覆盖。

- 执行步骤：
1. 让 `path->sha` 缺失（例如清空基线或构造无映射场景）。
2. 在本地与云端分别修改同一空白卡 `markdownSource`。
3. 执行同步。
- 预期：
1. 不自动三方 merge。
2. 按 `blankCardSourceMergeFallback` 进入 `conflict-copy` 或 `manual`。
3. 不发生无提示覆盖。
- 结果：`未测`
- 备注：____________

### R3 同步后索引推进

验收标准：`prevSyncIndex.files[path].sha` 与 `runtime.lastRemoteSha` 正确推进。

- 执行步骤：
1. 执行一次有文件变更的成功同步。
2. 检查 `prevSyncIndex` 与 runtime 状态。
- 预期：
1. 变更文件的 `path->sha` 更新到新值。
2. `runtime.lastRemoteSha` 与本轮远端提交一致。
- 结果：`未测`
- 备注：____________

### R4 第二次无改动 push = 0

验收标准：第二次无改动 push 变更数为 0（哈希与基线一致）。

- 执行步骤：
1. 先执行一次成功 push。
2. 不做任何本地修改，立即再次 push。
- 预期：
1. 第二次 push 显示无变更（changed=0 / no-op）。
2. 不产生额外提交。
- 结果：`未测`
- 备注：____________

## 2. 结论

- R1：`未测`
- R2：`未测`
- R3：`未测`
- R4：`未测`

总评：`未签收`

## 3. 问题单（可选）

- 问题 1：____________
- 问题 2：____________
