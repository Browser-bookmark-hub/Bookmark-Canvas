# Favicon 子域名通解计划书（Bookmark Canvas）

日期：2026-04-14  
范围：`Bookmark-Canvas`（侧栏、画布、搜索、树渲染）全部 Favicon 入口  
目标：在懒加载场景下仍保持统一算法，不做页面特调

## 1. 统一目标
1. 键模型：`hostname`（子域名）唯一键。
2. 存储模型：IndexedDB 为主缓存（`favicons`、`failures`）。
3. 请求模型：`inflight(hostname)` 去重 + 瀑布流兜底。
4. 渲染模型：占位图先显示，异步回填；数据查询仍走统一 resolver。
5. 速度优先：前台不阻塞等待 `await`，允许偶发 miss 直接走瀑布流覆盖回写。

## 2. 通解流程
1. URL -> 解析 `hostname`。
2. 查 `failures(hostname)`：TTL 内直接短路。
3. 同步阶段立即返回当前可用图标（无则占位），不阻塞等待异步链路。
4. 后台查 `favicons(hostname)`，命中则回填。
5. 缺失时走瀑布流。
6. 成功：写入 `favicons`（允许覆盖回写），并清理对应失败缓存。
7. 失败：写入 `failures(hostname, ts)`。

## 2.1 速度优先细则
1. 默认 `last-write-wins`，不做阻塞式质量比较。
2. 允许偶发“缓存本可命中但仍走网络”的覆盖回写。
3. `inflight(hostname)` 继续保留，避免同 host 请求风暴。

## 3. 懒加载场景如何保持“通解”
1. 画布只把“当前可视区 + 预读窗口”的 URL 交给通用批量 resolver。
2. resolver 内部逻辑与其他页面一致，不写画布专用缓存策略。
3. 这样既避免全量扫描，也不引入页面分叉算法。

## 4. 性能约束
1. 必须批量事务：去重 hostname 后，单 readonly 事务批量查询。
2. 禁止逐条事务查询（这是卡顿主因）。
3. 网络层只处理 miss 集合，并发受 inflight 去重控制。
4. 写回采用批量 readwrite 事务。

## 5. 风险与限制
1. 首次冷缓存仍受网络影响（不可避免）。
2. 外部 favicon 源可用性和返回质量不可控。
3. 子域名维度会增加存储条目，但可避免误复用。
4. 若失败 TTL 过长会延迟恢复，建议维持短 TTL（约 5 分钟）。
5. 覆盖回写策略会在极少数情况下引入短暂图标抖动。

## 6. 验收标准
1. 二次加载同画布区域时，以 IDB 命中为主。
2. 同一 `hostname` 并发请求只发一次网络请求。
3. 日志可区分：`idb_hit / network_fetch / failure_ttl_block`。

## 7. 回滚策略
1. 提供开关切回旧链路。
2. 出现兼容风险先回滚批量事务变更，再定位具体入口。
