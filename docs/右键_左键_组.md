## 完成情况

- [x] 1.1 画布空白区域右键：增加永久栏目副本到此位置
- [x] 1.2 画布空白区域右键：增加空白临时卡片
- [x] 1.3 画布空白区域右键：添加按钮（二级 UI，生成特殊临时栏目）
- [x] 1.4 画布空白区域右键：增加空白栏目卡片
- [x] 1.5 画布空白区域右键：创建固定卡片组
- [x] 2.1.1 目标对象右键：书签树类型（永久栏目/副本/临时栏目）
- [x] 2.1.2 目标对象右键：其他（空白栏目卡片/连接线）
- [x] 3.1.2 卡片组（首类化 + JsonCanvas 几何嵌套存储 + 渲染 + 工具窗 + 整组拖动）
- [x] 3.1.1 左键框选 + 临时组（蒙版整组拖动 + 工具窗 + 转 card-group）
- [x] 3.2.1 导入快照包转为普通 card-group（JsonCanvas group + 几何成员）
- [x] 3.2.2 左侧目录栏「组」区块：所有组平铺同级展示，不再区分来源
- [x] 3.3.3 卡片模式搜索：匹配 card-group 名字，点击候选定位

---

## 1、画布空白区域的右键

	- [x] 增加永久栏目副本到此位置（相当于点击「复制永久栏目」按钮即可）
	- [x] 增加空白临时卡片（就是没书签、没说明的那种，但是属于特殊临时栏目--添加）
	- [x] 添加按钮（点击后添加的二级UI）（到此位置形成一个特殊临时栏目--添加）
	---分割线
	- [x] 增加空白栏目卡片(Add card)（当前是双击就可以）
	- [x] 创建固定卡片组(Create group) 参考下方的「##3、」

### 2、目标对象的右键与左键
#### 2.1、目标对象右键

<u>右键的套路基本上和悬浮工具窗的功能复用，基本功能就是一个对应映射即可，比如：创建副本（相当于点击「复制永久栏目」按钮即可）但有例外；</u>
右键的面板ui需要根据缩放比例一起，参考当前的栏目卡片在缩放比例中的大小即可进行计算，这样符合视觉；

2.1.1、书签树类型：
	- 永久栏目: 全屏（再次点击若当前属于全屏就是退出全屏，相当于完整映射按钮，下面提到的「全屏」都相同）、定位（这个无映射，参考别的定位按钮怎么做的，下面提到的「定位」都相同）、置顶、创建副本（相当于点击「复制永久栏目」按钮即可，下面提到的「创建副本」都相同）、导出当前（json/html：json直接复用我们现有「导出单个文件夹」的json算法；html格式是纯书签树格式（调用api）（这个要说明），不带说明与tag，下面提到的「导出当前」都相同）
	- 永久栏目副本:全屏、 定位、置顶、删除（用红色字体，下面提到的「删除」都相同）、创建副本、导出当前（直接用永久栏目的那个按钮即可，参考## 4、）
	- 临时栏目类:全屏、颜色、定位、重命名、置顶、删除、导出当前（json/html：json直接复用我们现有「导出单个文件夹」的json算法；html只有标准的书签树格式，不带说明与tag标识等，这个要说明，参考## 4、）；普通链式或者特殊临时栏目我们导出文件的名字json/html应该参考我们全局导出的那样文件名字一样 中、英文等；



2.1.2、其他：
	- 空白栏目卡片:全屏、颜色、定位、置顶（当前悬浮工具窗也没有）、删除、复制当前栏目源码（其实就是此栏目存储的这个bcs源码）
	- 连接线:定位、删除


---


## 3、组(Create group)


### 3.1、==基础规则==：

分已经创建的卡片组，以及临时左键选中的临时组

3.1.1、临时组规则：

按住左键拖选框选选中（类似macos或者windows的鼠标左键拖选）之后，「临时」选中的那些（组内元素：卡片，永久栏目及其副本、临时栏目、空白栏目、组，或者组里面的一部分、连接线等框选的元素），框选选中后：
	1、组内的区域用一层临时蒙版遮住，然后按住蒙版的任意位置可以拖动移动整组包括组里的元素，一起移动；
	2、出现一个悬浮工具窗，创建卡片组（下面3.1.2、规则）、颜色、定位、置顶（临时组内元素置顶）、删除（删除组内元素），参考空白栏目/连接线的悬浮工具窗ui及其工具怎么做的即可；


3.1.2、卡片组


3.1.2.1、创建卡片组
「3.1.1、」的「创建卡片组」：当前左键框选的元素形成一个卡片组）；
「1.5、」的「创建卡片组」：创建一个空白的卡片组；

3.1.2.3、卡片组规则

1、卡片组内的空白区域不能按住拖动整组，只有按住左上角的名字的ui区块才可以移动整组，
2、卡片组和我们的临时栏目、空白栏目、永久栏目等一样都有对应的连接点，四周四边可以拖动具体ui与颜色规则参考临时栏目的四边ui或者连接点ui即可；且选中时边框高亮颜色规则也也是参考它即可；
3、（重要）卡片组规则，重要的一点就是嵌套；我们BCS里的`.canvas`存储、导出、导入（备份、下载备份、恢复等）必须全面转向并严格参考Obsidian/JsonCanvas的group格式：@/Users/kk/Downloads/参考/jsoncanvas-main/sample.canvas 也就是说，卡片组在`.canvas`里按几何嵌套表达，group节点只写自身的`id/type/x/y/width/height/label/color`等标准字段，不写children；成员归属由几何包含关系推断。
4、（重要）渲染参考图片@/Users/kk/Desktop/Screenshot 2026-05-23 at 14.26.34.png；
渲染方法是 obsidian的渲染方法，但是可以参考@@/Users/kk/Downloads/ 参考/xyflow-main 进行渲染，这个后续再调节；

3.1.2.4、卡片组的悬浮工具窗
 颜色、定位、置顶、删除组框、删除组框与组内元素，参考空白栏目/连接线的悬浮工具窗ui及其工具怎么做的即可（上方的3.1.1、也可以参考）



---

### 3.2、其他

#### 3.2.1、「导入快照包」现有代码需要修改

主要是导入的「导入快照包」
当前只是一个简易的group node，需要按照「3.1.2.3、的3、」全面转成JsonCanvas/Obsidian group语义；
实际交互与渲染也要改，可以根据我们「3.1.2.3、的4、」进程参考修改，也就是把它当作普通卡片组。注意我们不是生产版本，所以可以不用进行旧数据兼容；

#### 3.2.2、在左侧目录栏出现的东西
	- 有了卡片组存储的话，这种左侧的目录栏就在「其他」下方增加一个区块，名字是「组」或者「Group」，显示当前组的名字「默认名字是当前时间戳（参考临时栏目）」，点击后跳转至对应组的位置以及适当缩放（定位按钮的映射），组里的内容可以不用显示在这里（也就是没有折叠展开），并行显示，因为obsidain的嵌套基本上是无限嵌套，所以只能并行；
	- 「导入快照包」导入后直接成为普通卡片组，在目录侧只进入「组」区块，不再生成特殊分离区块；


#### 3.3.3、搜索
在「卡片（组）」模式里面，像书签模式一样提供「卡片 / 组」两个二级筛选按钮；不分全局模式或者全屏模式，可以直接搜索对应的「组」的名字，点击候选直接定位（相当于点击「定位」按钮）；如果当前处于全屏模式，点击组候选时先退出全屏模式，再定位到对应组的位置。


✅连接线规则：

元素：「永久栏目及其副本、临时栏目、空白栏目、卡片组、连接线」

卡片组内部的元素 不可有连接线 连接 其元素所属的卡片嵌套组的四个连接点，也就是凡是所属这个卡片组的元素（上面提到的）不能连接本卡片组连接点，因为已经是嵌套所属关系了。

另外连接线默认是单箭头的连接，不是无箭头都改一下；
两个节点，「来回」连接的话两次及以上就是双箭头节；

连接点所以卡片的地方 只要鼠标放在边界上就出现连接点？ 没必要 增加横向或者纵向连接点的识别百分比 70%吧，因为连接点是中心点，左35%右35%或者上35%下35%

---

元素：「永久栏目及其副本、临时栏目、空白栏目、卡片组、连接线」

✅嵌套卡片组的框体的置顶按钮现在有些问题： 应该是框体及内部元素都是置顶 然后再次点击这个按钮置顶取消 当前是有些问题 置顶不够？

以及临时卡片组（按住左键拖动的）它的置顶按钮，用的是元素里面的开关吧？

连接线不需要置顶，嵌套卡片组的框体需要置顶？没必要吧？
都利用本身的置顶按钮即可？就永久栏目（及其副本）、临时栏目（普通、特殊）、空白栏目卡片吧
无论是临时卡片还是嵌套卡片组

---
✅元素「空白栏目、卡片组、连接线」的悬浮工具窗参考他们右键的东西，独立于缩放比例外，ui大小

✅我们空白栏目卡片，它的悬浮工具窗的定位按钮，它的右边边增加一个置顶按钮吧。虽然说我们右键已经加入了。

---

✅目录--组的修改
1、本地运行的「组」统一只显示 card-group，既显示嵌套卡片组又显示同级元素，元素：「永久栏目及其副本、临时栏目、空白栏目、卡片组、连接线」只显示名字和所属图标（永久栏目前面不需要图标），内部嵌套组优先显示在最上方；
2、导入快照包不再生成特殊组类型，导入后直接进入普通卡片组；删除按钮保持「仅删除框体 / 删除框体及成员」两种行为；
3、目录栏取消旧的二级分类标题，所有条目只在「组」下方按正常名字数字与 A-Z 排序，不需要图标区分。

元素即使在组里，也同时保留在普通目录；组里面也照常显示一份。

导入、导出、本地存储的bcs组都是参考这样的嵌套组@/Users/kk/Downloads/参考/
jsoncanvas-main/sample.canvas 你可以看到，基本上就是导入进来就当作一个普通组


✅本轮完成：清理目录栏死代码、删除旧成员关系数组、统一搜索模块、导入/导出/本地存储均按 JsonCanvas 普通 group 处理.


组的排序默认放在目录里第一个默认只展开一级 除非用户手动折叠，永久栏目也是默认展开一级 除非用户手动折叠，其他默认折叠，除非用户手动折叠展开 存储记忆

✅组的一级标题点击后不要定位跳转，就是有折叠的那个，因为我们有定位放大按钮，鼠标悬浮在标题上就会出现；

---





✅
然后 bug 就是我们的这个卡片组左上角这个标题，它这个文字，它有的还是会变大，有的 UI 还会跟随着变大。但是，就比如说只有一两个字，就没有达到这个二分之一的时候，文字就会变得很大，这个我们记得以前有过相关的代码。
就是文字好像放大到一定程度固定大小还是什么意思，只接受1/2的？



✅搜索补入


---

#### 4、其他、 待大批量完善后做（当前不做）


✅2.1.3、组（group）的右键

2.1.3.1、卡片组的右键（参考3.1.2、），在卡片组空白的区域、边框、标题甚至是选中状态的时候都可以出现右键面板（具体怎么做参考，空白栏目卡片的右键面板，基本上是一个功能映射）
	- 颜色（参考临时栏目的颜色以及渲染规则，下同）、定位、置顶、删除（只是删除组框，取消嵌套）

2.1.3.2、按住左键选中临时的选中区组的右键（参考3.1.1、）；
	- 创建卡片组、颜色、定位、置顶、删除（删除是选中区组里面的内容）


✅目录栏：取消旧分类标题，只要在「组」下方显示所有卡片组即可。

---

✅帮我修复一下我们就是组的这个内嵌组。它调节就是点击边框进行调节它的长宽的时候无法调节，就是四个边或者四个角。但是它外边的那个就是它组外边的，就是包裹它的这个组，它却可以调节。



✅测试全场导出、导入、存储测


4.1、单文件/文件夹导出
4.1.1、单文件的导出
在上面「2.1.1、」提到的右键的导出，主要是针对 永久栏目、临时栏目的json或者书签树状结构的html导出。
	即：「json直接用我们当前「导出单个文件夹」的json算法即可，不直接复用当前全屏导出的`fullscreen-json`语义；html针对永久栏目的话就是调用chrome bookmark api拿到json然后转html，临时栏目则从内部书签树结构转html。html一律是标准书签树格式，不带说明与tag。」
我们当前已经做好了，json的导出的文件 名字和里面的数据 和我们按下「导出」导出全局的时候是一样的。只需要做「4.2、」的单文件的导入即可。

4.1.2、卡片嵌套组 / 临时左键框选组 的 右键的导出
和整体的「导出」、「导入」用一套系统，右键的面板最下方加入一个「导出」但是导出的最小单位缩小成当前卡片嵌套组，有一个小型的.canvas；
而临时左键框选的那些也是作为对应的导出，参右键的面板最下方加入一个「导出」考完整导出的格式，有一个小型的.canvas；


4.2、单文件 / 组 的导入

1、「4.1.1、」说了，针对上面两文件的在「导入」ui里面的下面两个按钮「书签文件」的json/html格式的适配，作为特殊临时栏目导入；
2、画布空白的地方进行右键，右键面板里最下方加入「此位置导入」，点击后出现「导入面板」，二级ui面板名字临时改为（此位置导入）然后通过右键面板进入的「导入面板」里面的「覆盖」选项隐藏，二也就是文件夹直接就是快照包导入。然后关键点来了，在画布空白的地方右键的时候记录坐标，然后点击导入后出现的对象都在此位置出现。

✅


✅单文件导入也就是「书签文件
HTML 书签
JSON 书签/临时」这两个，无论是「导入」还是「此位置导入」，在选择的时候可以选择批量吧，然后都是并排的，若超过5个就换行；


修改一下导入的ui，在「画布快照

文件夹

压缩包」的文件夹和压缩包的横向宽度参考下方的「签文件

HTML 书签

JSON 书签/临时」
